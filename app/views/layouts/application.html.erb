<!DOCTYPE html>
<html>
  <head>
    <title>Nekko.ch | <%= @title || 'Смотреть аниме' %></title>
    <meta name="HandheldFriendly" content="true">
    <meta name="format-detection" content="telephone=no">
    <meta name="viewport" content="user-scalable=0, initial-scale=1.0, maximum-scale=1.0, width=device-width">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.10.1/css/all.css" integrity="sha384-wxqG4glGB3nlqX0bi23nmgwCSjWIW13BdLUEYC4VIMehfbcro/ATkyDsF/AbIOVe" crossorigin="anonymous">
  </head>

  <body>
<!--    <div id="p_prldr">-->
<!--      <div class="contpre"><span class="svg_anm"></span><br>Подождите...<br><small>идет загрузка</small></div>-->
<!--    </div>-->

    <div class="wrap">
      <%= render 'shared/header' %>

      <div class="main center container">
        <div class="cols clearfix">
          <div class="cont">
            <%= yield %>
          </div>
          <!-- end cont -->
          <aside class="side" id="side">
            <div class="side-b">
              <div class="side-bt">Последние обновления</div>
              <div class="side-bc">

                <% Anime.order(updated_at: :desc).limit(5).each do |anime| %>
                  <div class="ser">
                    <%= link_to anime.title, anime_path(anime.id), class: "ser-t nowrap" %>
                    <div class="ser-s">
                      <%= anime.genres.join(', ') %>
                    </div>
                    <div class="ser-d">
                      Обновлено
                      <% if anime.updated_at.today? %>
                        сегодня <%= anime.updated_at.strftime("%H:%M") %>
                      <% else %>
                        <%= anime.updated_at.strftime("%d.%m %H:%M") %>
                      <% end %>
                    </div>
                    <div class="ser-i img-box ps-link" data-link="<%= anime_path(anime.id) %>">
                      <span class="topicImage" style="background-image: url(<%= anime.posters[0] %>);"></span>
                      <span class="fa fa-play-circle"></span>
                    </div>
                  </div>
                 <% end %>

              </div>
            </div>
            <div class="side-b">
              <div class="side-bt">Наивысший рейтинг</div>
              <div class="side-bc">

                <% Anime.order(rating: :desc).limit(5).each do |anime| %>
                  <div class="ser">
                    <%= link_to anime.title, anime_path(anime.id), class: "ser-t nowrap" %>
                    <div class="ser-s">
                      <%= anime.genres.join(', ') %>
                    </div>
                    <div class="ser-d">
                      Рейтинг <%= anime.rating.round(1) %> из 10
                    </div>
                    <div class="ser-i img-box ps-link" data-link="<%= anime_path(anime.id) %>">
                      <span class="topicImage" style="background-image: url(<%= anime.posters[0] %>);"></span>
                      <span class="fa fa-play-circle"></span>
                    </div>
                  </div>
                <% end %>

              </div>
            </div>
            <div class="side-b">
              <div class="side-bt">Случайные аниме</div>
              <div class="side-bc">

                <% Anime.order("RANDOM()").limit(5).each do |anime| %>
                  <div class="ser">
                    <%= link_to anime.title, anime_path(anime.id), class: "ser-t nowrap" %>
                    <div class="ser-s">
                      <%= anime.genres.join(', ') %>
                    </div>
                    <div class="ser-d">
                      Рейтинг <%= anime.rating.round(1) %> из 10
                    </div>
                    <div class="ser-i img-box ps-link" data-link="<%= anime_path(anime.id) %>">
                      <span class="topicImage" style="background-image: url(<%= anime.posters[0] %>);"></span>
                      <span class="fa fa-play-circle"></span>
                    </div>
                  </div>
                <% end %>

              </div>
            </div>
          </aside>
          <!-- end side -->
        </div>
        <!-- end cols -->
      </div>

      <%= render 'shared/footer' %>
    </div>



  </body>
</html>
